# Postgres connection (Neon via Vercel Marketplace, or any Postgres)
DATABASE_URL="postgresql://user:password@host:5432/pixeltech?sslmode=require"

# Auth.js
# Generate with: openssl rand -base64 32
AUTH_SECRET="change-me-to-a-long-random-string"

# Only needed when deploying
AUTH_URL="http://localhost:3000"

# Admin bootstrap (seed script reads these)
ADMIN_EMAIL="admin@pixeltech.dev"
ADMIN_PASSWORD="ChangeMeNow!2026"
